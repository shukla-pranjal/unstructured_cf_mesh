# New Mission

- Add an option for a file opener (open export directory).
- Add support for changing fluid (at least 2 liquids, e.g., Air and Water) which will automatically update the kinematic viscosity.